# DESABILITADO INTENCIONALMENTE.
#
# Nenhum deploy Portainer deve compilar a aplicação no servidor. O runtime
# oficial usa deployments/portainer/stack.yaml e imagens GHCR publicadas.
#
# Para desenvolvimento/build local explícito, use exclusivamente:
#   docker compose -f compose.yaml -f compose.local-build.yaml up -d --build
#
# Esta variável obrigatória faz qualquer tentativa acidental de usar este
# arquivo falhar antes de criar containers.
x-disabled: ${ARGWS_DO_NOT_USE_PORTAINER_STACK_BUILD:?stack-build.yaml desabilitado; use stack.yaml ou compose.local-build.yaml apenas localmente}

services: {}
